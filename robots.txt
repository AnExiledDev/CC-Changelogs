# This site is retired. The changelogs live at
# https://changelogs.core-directive.com/ and every page here carries a
# cross-site canonical pointing at its counterpart there.
#
# Crawling is deliberately allowed: a blanket Disallow would hide the
# per-page "noindex, follow" and the canonical from the crawler that needs
# to read them.
User-agent: *
Allow: /

Sitemap: https://changelogs.core-directive.com/sitemap.xml
