# This is the PUBLISHED site repo (AnExiledDev/CC-Changelogs).
# Only generated HTML + CSS belongs here. The build system lives in
# harness-source/tools/changelog_site/ and must never be published here.

# Build scripts / tooling
*.py
__pycache__/
*.pyc

# Scratch / local-only
_mockups/
*.zip
*.tmp

# OS cruft
.DS_Store
Thumbs.db
